export interface Pagination {
    currentPage: number;
    itemsPerPage: number;
    totalItems: number;
}

export interface Image {
    title: string;
    url: string;
    sourceUrl: string;
    description: string;
}
